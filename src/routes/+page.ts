import type { PageLoad } from "./$types";

export const load: PageLoad = () => {
    return {
        title: "Registro - RLM-MUN",
        description: "¡Forma parte del RLM-MUN! Regístrate para formar parte de esta nueva historia."
    };
};
