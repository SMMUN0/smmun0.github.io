import type { PageLoad } from "./$types";

export const load: PageLoad = () => {
    return {
        title: "Registro Faculty - RlM-MUN",
        description: "¡Te estamos buscando! No dudes el ser parte de una experiencia extraordinaria, regístrate en el RLM-MUN."
    };
};
